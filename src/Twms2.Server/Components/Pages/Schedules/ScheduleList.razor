@page "/schedules"
@inject NavigationManager Navigation
@attribute [Authorize(Roles = "Admin")]

@code {
    protected override void OnInitialized()
    {
        Navigation.NavigateTo("/admin/config", replace: true);
    }
}
